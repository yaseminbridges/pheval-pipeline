process {
    executor = 'sge'
    queue    = 'all.q'

    clusterOptions = '-cwd -j y -pe smp 4 -l h_vmem=16G -l h_rt=01:00:00'
    container = "sif/pheval-exomiser-0.6.5.sif"
}
singularity {
    enabled    = true
    autoMounts = true
}
